# Download gfs weather forecast data for La Palma

## Requirements

The grib format is somewhat special.
I recommend to use anaconda with python 3:

```
conda install pandas pyproj requests
pip install pygrib
```
